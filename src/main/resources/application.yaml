server:
  port: 8085

spring:
  application:
    name: Spring-Boot-CRUD-Operation-Pet
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pet_db
    username: root
    password: Tiger2143@+%
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
